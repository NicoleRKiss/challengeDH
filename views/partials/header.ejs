
 <div class="log">
    <% if(typeof user == 'undefined') { %>
    <a class="navLink" href="/register/login">Iniciar Sesion</a>
    <a class="navLink" href="/register">Registrate</a>
    <% } else { %>
   <h1>HOLA <%= user.email %></h1>
    <form action="/register/logout" method="POST" class="p-2">
        <button  type="submit">
            Cerrar sesión <i class="fas fa-sign-in-alt"></i>
        </button>
    <% } %>
</div>
